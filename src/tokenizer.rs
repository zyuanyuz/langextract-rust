//! Text tokenization functionality.

/// Placeholder for tokenization functionality
pub struct Tokenizer;

impl Tokenizer {
    /// Create a new tokenizer
    pub fn new() -> Self {
        Self
    }
}

impl Default for Tokenizer {
    fn default() -> Self {
        Self::new()
    }
}
